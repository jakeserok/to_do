<%= form_with model: [@goal, @goal.tasks.build] do |f| %>
  <p>
    <%= f.label :item %>
    <%= f.text_field :item %>
  </p>
  <p>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.submit %>
  </p>

<% end %>